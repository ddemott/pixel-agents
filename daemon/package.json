{
  "name": "pixel-agents-daemon",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "dist/server.js",
  "bin": {
    "pixel-agents-daemon": "dist/server.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "start": "node dist/server.js",
    "clean": "rm -rf dist"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  }
}
